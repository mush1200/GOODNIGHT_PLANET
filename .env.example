# Goodnight Planet — environment reference (no real secrets in this file)
# Copy to apps/api/.env or apps/mobile/.env and fill values locally.
# Production: Railway Variables (API) / EAS Secrets (mobile).

# API (Railway + Supabase)
DATABASE_URL=
JWT_SECRET=
PORT=
NODE_ENV=production

# PUBLIC_API_URL=
# CORS_ORIGINS=

# Mobile (Expo)
EXPO_PUBLIC_API_URL=
